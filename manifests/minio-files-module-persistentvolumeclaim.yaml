apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: minio-files-module
  name: minio-files-module
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
